function logVariables() {
	if(true) {
		var x = 1;
		let y = 2;
		const z = 3;

        console.info('X: ' + x);
        console.info('Y: ' + y);
        console.info('Z: ' + z);
    }

    // console.info('X: ' + x);
    // console.info('Y: ' + y);
    // console.info('Z: ' + z);

}

// console.info('X: ' + x);

logVariables();
